---
name: intent-status
description: Show a quick status summary for the active Intent changeset — metadata, todos, PRs, and local workspace state.
---

# Intent Status

Quick dashboard for the current (or specified) changeset.

Follow the **status-changeset** skill exactly. Apply shared patterns from the **intent-changeset** rule.

## Arguments

- **`$1` (optional)** — Changeset UUID or short ID. When provided, use it directly in Step 1 of the skill.
